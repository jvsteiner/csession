{
  "name": "csession",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "csession": "./dist/cli.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test 'dist/**/*.test.js'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
